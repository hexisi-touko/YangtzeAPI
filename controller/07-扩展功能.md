# 扩展功能

这里放与主链路相对独立的用户体验功能。它们可以在掌握登录、渠道、中转和额度后再看。

- `rankings.go`：用量或用户排行相关接口。
- `checkin.go`：签到状态和签到操作。

阅读方法：先在 `router/api-router.go` 找 URL，再从 controller 看返回数据，最后去 `model/` 找统计字段或记录表。课题组自用时，这些功能不是跑通中转链路的前置条件。
