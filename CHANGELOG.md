# 版本更新日志 (Changelog)

本文档详细记录本次推送相较于 GitHub 仓库远程版本（`origin/main` 与 `origin/master`）的全部改动与优化项。

---

## 桌面客户端更新清单 (`main` 分支)

### 1. 界面与交互重构（全面向 CC-Switch 设计语言看齐）
- **平铺式 Agent 列表架构**：
  - 彻底移除旧版冗余的左右侧边栏分割，采用与 CC-Switch 原版一致的平铺卡片列表；
  - 每个 Agent（Codex、Claude Code、Gemini）独立卡片，实现“一个 Agent 对应一个专属账号”的简洁逻辑。
- **纯白无边框沉浸式窗口**：
  - 去除了 Windows 系统自带的黑边标题栏，改为自绘纯白标题栏；
  - 包含原生窗口控制按钮组（最小化 `−`、最大化/还原 `□`、关闭 `×`）与全区域安全拖拽移动；
  - 左上角品牌 Logo 升级为用户专属矢量图标（采用 `长大实验室.svg`），尺寸适配放大，视觉效果极其精致；
  - 支持浅色（默认纯净白色）与深色（极客暗黑）两套主题无缝切换，并自动记忆用户选择。
- **激活态与蓝边逻辑对齐**：
  - 规范了高亮逻辑：未启用的卡片保持干净的灰白边框；**只有当前真正启用的 Agent 才会点亮蓝色高亮外框（蓝边）**，主按钮同步切换为绿色的“启动客户端”；
  - 一眼即可直观分辨哪个 Agent 处于当前生效状态。

### 2. 1:1 复刻的供应商配置详情弹窗 (Modal)
- 之前主界面硬铺大表单导致界面杂乱，现重构为标准的弹窗交互：点击卡片右侧的 **`✏️`（编辑/详情图标）** 即刻呼出；
- **100% 严格复刻 CC-Switch 完整表单字段**：
  1. 顶部居中圆角大图标与图标背景盒；
  2. 供应商名称 与 备注；
  3. 官网链接：统一自动绑定并展示当前 New API 服务端点（如 `http://127.0.0.1:3000`）；
  4. API Key：**默认明文清晰展示**用户分配的真实 Key（点击小眼睛可自由切换隐藏/显示，支持一键复制），下方附带原版提示文字；
  5. API 请求地址：展示真实端点，配备完整 URL 开关、一键复制按钮、以及黄色灯泡警告提示框（`💡 填写兼容 OpenAI Response 格式的服务端点地址`）；
  6. 默认模型：配备预设图标及提示；
  7. 高级选项卡片（折叠面板）：
     - 上游格式：原生下拉框展示 `Responses（原生）` 及对应协议说明；
     - 模型映射：**根据服务器实际开通的模型列表动态生成**，列出各可用模型及其同步状态；
     - 自定义 User-Agent：带预设下拉徽标及生效提示；
     - 本地代理请求覆盖：并排呈现 Header 覆盖 与 Body 覆盖代码盒；
     - auth.json (JSON) \*：原版高亮代码框，带格式化按钮，实时注入用户真实 Key；
     - config.toml (TOML)：带远程压缩、通用配置、1M 上下文窗口、压缩阈值 900000 选项，下方代码框完整渲染真实 TOML 配置；
- **核心保障**：弹窗内全部参数均为**只读锁定防篡改**，用户能看、能查、能复制，但无法键盘修改，严格保障团队分发规则。

### 3. 数据流与通信底层修复
- **解决鉴权失败与 API Key 为空问题**：
  - 修复了客户端登录成功后后续查询请求未携带 `Authorization: Bearer <access_token>` 请求头导致的 401 报错；
  - 确保客户端可稳定解密提取用户专属唯一的 API Key，并直接灌入详情弹窗与配置文件中。
- **基于服务器 URL 动态嗅探模型**：
  - 废弃界面硬编码的死数据模型名；
  - 登录后自动调用 `/v1/models` 接口，探测上游服务真实开通的模型清单，并自动为 Codex 匹配可用的实际模型（如 `gpt-5.6-luna`）。
- **彻底根治 Codex 启动报错 “Windows 安装未完成 • config_load”**：
  - 诊断出 Codex 对 `~/.codex/config.toml` 的严格语法校验机制；
  - 重写了 `mergeToml` 算法：将顶级键（`model_provider = "custom"`、`model`、跳过 WSL 检查的 `windows_wsl_setup_acknowledged = true`）统一规范写入文件最顶层；
  - 移除多余和格式混乱的旧残留块，独立在末尾注入合规的 `[model_providers.custom]` 配置段，彻底消除语法冲突与系统报错。

---

## 服务端与管理端更新清单 (`master` 分支)

- **新注册用户自动额度注资**：
  - **背景**：原 New API 注册审核通过后仅生成 Token，但用户账号自身额度为 0，导致新用户一调用接口即被阻断报错（`用户额度不足, 剩余额度: ＄0.000000`）；
  - **修复**：修改 `model/user_application.go` 的 `ApproveUserApplication` 逻辑：管理员在审核批准申请时，在为账号生成唯一 Key 的同时，**自动为该用户注入初始可用额度**；
  - 确保新注册用户通过审核后，无需管理员再手动去充值/赠送额度，开箱即可直接调通模型对话！
