# YangtzeAPI 配套服务端更新

## 2026-09-05

配合 `main` 分支客户端 1.1.0 源码更新；本次不发布公网服务或安装包。

### 修复

- 在 Responses 请求转换后，对 Kimi/Moonshot 函数工具参数中的 `$ref` 同级约束作兼容处理，避免 Moonshot Schema 校验拒绝请求。
- 使用 `allOf` 保留引用与同级约束，保留已有组合、递归引用和 default/examples/enum/const 中的字面数据。
- 支持命名空间及嵌套函数定义；非 Kimi/Moonshot 请求和原始透传模式不作转换。
- 增加对应 Schema 回归测试。

### 部署说明

- 本地修改不会自动更新公网服务；目前尚未部署公网服务器。
- 从更新后的源码重新构建服务端，再验证模型路由、Responses/Claude Messages 转换和工具调用。
- 用户审批、令牌和额度沿用现有服务端功能。模型列表受网关令牌权限控制，客户端展示模型并不证明所有协议都可用。
