# YangtzeAPI 演示与验收指南

当前客户端内置 Claude Code、Codex。Kimi 通过授权模型选择使用，独立 Kimi 聊天与 Gemini 卡片已删除。

## 演示准备

按 [README](README.md) 启动服务端与客户端。首次部署创建管理员，配置可用渠道、模型路由、令牌权限和额度。安装演示需要的外部 Agent。

本地测试使用 `127.0.0.1:3000`，只能证明本机链路可用。不要把本机账号、数据库、令牌或本地配置提交到仓库。

## 用户流程

1. 新普通用户提交注册申请，确认待审核状态。
2. 管理员审核通过，检查账号、令牌和初始额度。
3. 用户登录，确认 Claude Code、Codex 卡片和服务地址。
4. 分别获取模型，确认列表符合令牌权限，模型无需勾选。
5. 选择默认模型，保存后重新打开配置，确认选择保留。
6. 在干净配置下确认 Agent 初始未启用；点击启用后按钮为灰色，文字仍为“启用”。已有匹配本地配置时可以显示已启用。
7. 启动实际 Agent，验证对话、工具调用及结果回传；分别验证 Claude Messages 和 Codex Responses 路径。
8. 切换账号，确认模型偏好隔离；测试刷新失败提示。

卡片只展示链接，不展示底层模型名称。默认模型允许用户自行选择。

## 正式发布验收

1. 部署可访问的 HTTPS 服务，正式客户端配置不得指向本机。
2. 运行 `npm run build:production`，确认检查、测试和构建全部成功。
3. 在另一台 Windows 电脑安装对应 Agent 和管理客户端，完整重复注册、审批、登录、选择模型、启用及真实调用流程。
4. 检查断网、服务不可达、无授权模型、额度不足和渠道失败时的提示。
5. 验证完成后发布版本说明、安装包和校验值。

未知模型的推理强度、图片、搜索、上下文长度和工具能力应逐项验证；模型名称和网关回显不能独立证明上游内部模型身份。
