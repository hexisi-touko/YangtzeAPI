# YangtzeAPI 演示与开箱即用交付手册

本文档为 **长大计科智慧油田 · AI 终端管理系统** 的标准演示流程与纯净环境交付验收手册。

---

## 一、系统架构与角色边界

- **管理端（服务端）**：
  基于定制版 New API 构建，监听 `http://127.0.0.1:3000`。负责成员账号审核、全局唯一 API 令牌自动签发、初始额度注入与上游模型渠道路由。
- **用户端（桌面 EXE）**：
  基于 Electron 构建的极简 AI 终端管理工具。采用 CC-Switch 风格平铺视图，针对每个 Agent（Codex、Claude Code、Gemini）实现单账号单 Key 绑定，用户端只读锁定防篡改，一键写入配置并唤起本地应用。

---

## 二、初期准备

在正式演示前，请确保完成以下准备：

### 1. 管理端服务启动
在服务端源码目录（`master` 分支）中启动 Docker 服务：
```powershell
docker compose -f compose.local.yaml up -d --build
```
- 服务就绪后，在浏览器中打开：`http://127.0.0.1:3000` 或 `http://localhost:3000`
- 登录超级管理员账号（默认 `Tsuki`），进入后台管理界面。
- **关键配置检查**：在管理后台的 **【渠道】** 菜单中，确保已存在至少一条启用的上游模型渠道（需支持 `gpt-5.6-luna`、`gpt-5.5` 或相关模型），确保网络连通测试正常。

### 2. 用户端程序获取
从本仓库构建或 Releases 处获取最新安装包：
- **安装版**：`API-Client-LocalTest-Setup-1.0.0-x64.exe`（演示安装流程使用）
- **便携免安装版**：`API-Client-LocalTest-Portable-1.0.0-x64.exe`（即开即用）

---

## 三、演示阶段（9 步标准闭环）

按照以下步骤依次向评委/团队演练：

### 步骤 1：安装用户端 EXE
- 运行 `API-Client-LocalTest-Setup-1.0.0-x64.exe`，按照向导完成安装；
- 桌面生成带有专属“长大实验室”图标的快捷方式；双击打开，展示小巧整洁的纯白无边框登录窗口。

### 步骤 2：用户注册账号，发送审核请求
- 在登录窗口下方点击 **「注册新账号」**；
- 输入用户名（如 `demo_user`）、密码以及注册理由（如 `智慧油田项目组模型接入申请`）；
- 点击 **「提交申请」**，界面友好提示：*“申请已提交，等待管理员审核”*。

### 步骤 3：管理员端审核通过，账号与令牌自动创建
- 切换到浏览器中的管理员后台（`http://127.0.0.1:3000`）；
- 点击左侧菜单 **【用户管理】→【注册审核】**；
- 找到刚刚提交的 `demo_user` 申请条目，点击 **「批准」**；
- **系统自动执行**：
  1. 激活该用户账号；
  2. 自动为该账号注入初始可用额度；
  3. 自动生成并绑定该账号唯一的专属 API Key，无需管理员手动去令牌页面重复创建。

### 步骤 4：用户端登录账号
- 回到桌面的用户端 EXE 界面；
- 输入刚刚审核通过的账号与密码，点击 **「登录」**；
- 验证通过，登录窗平滑关闭，主工作台随即呈现。

### 步骤 5：进入用户端主界面
- 整个界面采用纯白沉浸式外观，消除 Windows 原生黑边，左上角呈现高清晰度专属实验室 SVG 徽标；
- 界面取消复杂的左右侧边栏分割，采用直观的 CC-Switch 风格平铺卡片列表，展示 `Codex`、`Claude Code`、`Gemini` 等对应 Agent 卡片。

### 步骤 6：强调单账号单 Key 绑定（只读防篡改）
- 点击 Codex 卡片右侧的 **`✏️`（详情图标）**，展开 1:1 复刻的完整供应商配置弹窗；
- **核心亮点解说**：
  - **API Key**：已全自动明文填入管理员审核发放的唯一真实 Key（支持复制与小眼睛切换）；
  - **API 请求地址**：自动绑定至当前服务网关 `http://127.0.0.1:3000/v1`；
  - **默认模型与模型映射**：并非死数据，而是根据所连服务真实开通的模型（如 `gpt-5.6-luna`）动态探测生成；
  - **官网链接**：已统一指向 New API 服务端点；
  - **auth.json / config.toml**：全自动动态渲染成标准配置内容；
  - **全局只读锁定**：输入框无法键入修改，杜绝人为错配，保障全团队配置一致性与安全性。

### 步骤 7：点击运行 Codex
- 关闭弹窗，在主卡片右侧点击大号蓝色按钮 **`▷ 启用`**；
- 客户端在后台将格式校验无误的配置原子写入本机的 `~/.codex/config.toml` 与 `auth.json`；
- 此时卡片外圈亮起**优雅的蓝色激活光晕（蓝边）**，按钮变绿并切换为 `启动客户端`；
- 系统自动唤起电脑里安装的 Codex / ChatGPT 桌面窗口，完全避免了 `config_load` 语法冲突与 WSL 报错。

### 步骤 8：发送测试消息
- 在唤起的客户端输入框中键入：
  ```
  你好，请告诉我你是什么模型
  ```
- 按下回车发送请求。

### 步骤 9：测试成功返回模型回复
- 请求经过本地 New API 网关进行鉴权、并发管控与额度校验，上游渠道成功返回回复（如：*“你好！我是基于 gpt-5.6-luna 架构的模型……”*）；
- 回到用户端界面，点击卡片右侧的 **`📊`（额度图标）**，可实时查看到调用次数累加、额度实时扣减，全流程闭环验证完毕！

---

## 四、纯净机测试注意事项（0配置开箱即用验证）

为确保给外部人员或评审展示时万无一失，请遵循以下纯净环境验证规范：

1. **先行自测**：
   - 务必在当前开发电脑上先跑通上述 1~9 步全部流程；
   - 检查电脑中 `C:\Users\<用户名>\.codex\config.toml` 文件是否正确切换为当前登录用户分配的专属 Key。
2. **纯净环境（0配置主机）演练**：
   - 找一台从未安装过 Codex、未配置过任何环境变量和代理的 Windows 电脑；
   - 将安装包拷贝至该电脑并执行安装；
   - 测试在没有任何系统全局配置的前提下，能否仅靠点击客户端中的「▷ 启用」直接一键写入配置并成功唤起客户端完成问答；
   - 验证是否达到真正的“开箱即用”效果。
3. **正式演示前清理**：
   - 纯净机测试成功后，若需要保留该机器作为正式演示用机，请卸载或清理测试配置（清除 `~/.codex/config.toml` 中 managed 区块），以备正式演示时现场展现“从 0 开始安装注册审核到即刻可用”的惊艳效果。
